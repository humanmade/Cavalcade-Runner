#!/bin/bash

set -eux

target=work/cavalcade-runner

rm -fr "$target" "$target.zip"
mkdir -p "$target"
cp -R bin fakewp inc vendor bootstrap.php "$target"
rm -fr "$target/bin/cavalcade.php" "$target/bin/README.md"
{
  cd work
  zip -r cavalcade-runner.zip cavalcade-runner
}
